services:
  verzoekjesbever:
    image: ghcr.io/vishengel/verzoekjesbever:latest
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./cache:/app/cache
    restart: unless-stopped
